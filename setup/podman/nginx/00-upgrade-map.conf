# WebSocket-Upgrade-Map (http-Ebene). Setzt den Connection-Header auf
# "upgrade", wenn der Client ein Upgrade anfragt, sonst auf "" (keep-alive).
# Wird von /sub_id/ (nchan WebSocket) in edtech.conf genutzt.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
